"""Question resolution module.

Determines what the user actually means by resolving pronouns and omitted
subjects using conversation memory, then passes a self-contained canonical
question to the research pipeline.
"""
